/*
 * ProfileHeader Messages
 *
 * This contains all the text for the ProfileHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProfileHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProfileHeader component!',
  },
});
