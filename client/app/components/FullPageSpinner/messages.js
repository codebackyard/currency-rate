/*
 * FullPageSpinner Messages
 *
 * This contains all the text for the FullPageSpinner component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FullPageSpinner';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FullPageSpinner component!',
  },
});
